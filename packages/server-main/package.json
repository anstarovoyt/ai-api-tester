{
  "name": "@api-tester/server-main",
  "version": "2026.1.0",
  "private": true,
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "build:test": "tsc -b test/tsconfig.json",
    "test": "npm run build && npm run build:test && node --test dist-test/*.test.js",
    "start": "node dist/index.js",
    "dev": "npm run build && npm run start"
  }
}
